name: Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# A queued deploy should replace a pending one rather than stack up behind it.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    # A project site is served under /<repo>/, so every root-relative link needs that prefix.
    env:
      BASE: /aorf
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - run: pip install -e .

      - name: The demo must be real, so it has to validate first
        run: |
          for dir in examples/*/; do
            aorf check "$dir" --strict
          done

      - name: Build the website
        run: python docs/build_site.py --out _site --base "$BASE"

      - name: Build the live demos with aorf build
        # Not a mockup: these pages are the actual tool's output over the example repos.
        run: |
          for dir in examples/signup-conversion examples/topic-clustering examples/minimal-spike; do
            name="$(basename "$dir")"
            aorf build "$dir" --out "_site/demo/$name" --base "$BASE/demo/$name"
          done

      - name: Fail if any demo the Examples page links to is missing
        run: |
          for name in signup-conversion topic-clustering minimal-spike; do
            test -f "_site/demo/$name/index.html" || {
              echo "missing _site/demo/$name/index.html"; exit 1;
            }
          done

      - uses: actions/configure-pages@v5
        with:
          # Creates the Pages site and sets its source to this workflow on the first run, so
          # the repository provisions its own Pages rather than needing a settings toggle
          # someone has to remember. Harmless once it already exists.
          enablement: true

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
