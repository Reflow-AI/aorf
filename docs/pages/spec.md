# AORF v0.1 specification

The frozen v0.1 specification, reproduced verbatim from
[`spec/AORF-v0.1.md`](https://github.com/Reflow-AI/aorf/blob/main/spec/AORF-v0.1.md) in the
repository — this page is generated from that file, so the two cannot drift.

**Machine-readable:** [`aorf-v0.1.schema.json`](./spec/aorf-v0.1.schema.json). It expresses
types, enums and unconditionally required fields only. Conditional requirements and every
cross-document rule are enforced by `aorf check`, because no JSON Schema can check that a
`baseline_value` matches the document it refers to.

Specification text is licensed [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/); the
`aorf` package is MIT.

<hr>

{{SPEC}}
